<% if signed_in_user? %>
  Welcome, <%= link_to "#{current_user.first_name} #{current_user.last_name}", current_user %>!
  <%= link_to "Logout", logout_path, :method => :delete %>
<% else %>
  <%= link_to "Login", login_path %>
<% end %>
