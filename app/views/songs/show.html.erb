<table>
  <tr>
    <th>ID</th>
    <th>Name</th>
    <th>Artist</th>
    <% if signed_in_user? %>
      <th>Playlists</th>
      <th>Add to Playlist</ht>
      <th>Bookmark</th>
    <% end %>
  </tr>
  <tr>
    <td><%= @song.id %></td>
    <td><%= @song.name %></td>
    <td><%= link_to @song.artist.name, artist_path(@song.artist) %></td>
    <% if signed_in_user? %>
        <td></td>
        <td></td>
        <td></td>
    <% end %>
  </tr>
</table>
